const csvData = "John,Doe,30,New York";

// Split the CSV data into an array using a comma as the separator
const dataArray = csvData.split(',');

console.log(dataArray);
// Output: ["John", "Doe", "30", "New York"]
